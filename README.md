# 🕸️ Graph Studio

**Модульная система создания, редактирования, визуализации и встраивания интерактивных графов связей на Canvas 2D с псевдо-3D-проекцией, физикой, органической пульсацией, потоками по ветвям, JSON-контрактом для ИИ и необязательной интеграцией с MCP.**

_Работает в современном браузере. Не требует фреймворка. Может запускаться как статический сайт, Docker-контейнер, Web Component, iframe или локальный редактор._

![Version](https://img.shields.io/badge/version-v8.0.0-263846)
![JavaScript](https://img.shields.io/badge/JavaScript-ES%20Modules-F7DF1E)
![Canvas](https://img.shields.io/badge/Renderer-Canvas%202D-6d7b88)
![Docker](https://img.shields.io/badge/Docker-ready-2496ED)
![MCP](https://img.shields.io/badge/MCP-optional-8A2BE2)
![Status](https://img.shields.io/badge/status-active-success)

---

## 📑 Содержание

1. [📝 Описание](#-описание)
2. [🎯 Для чего нужна программа](#-для-чего-нужна-программа)
3. [✨ Ключевые функции](#-ключевые-функции-и-возможности)
4. [🧩 Состав проекта](#-состав-проекта)
5. [🏗️ Архитектура](#️-архитектура)
6. [🧠 Модель данных](#-модель-данных)
7. [🕸️ Узлы и связи](#️-узлы-и-связи)
8. [🪐 Раскладки](#-раскладки)
9. [🧲 Физика и движение](#-физика-и-движение)
10. [❤️ Органическая пульсация](#️-органическая-пульсация)
11. [💨 Поток по ветвям](#-поток-по-ветвям)
12. [📐 Сужающиеся связи](#-сужающиеся-связи)
13. [🖥️ Интерфейс](#️-интерфейс)
14. [👁️ Режим просмотра](#️-режим-просмотра)
15. [🤖 Работа с ИИ без MCP](#-работа-с-ии-без-mcp)
16. [🔌 MCP-sidecar](#-mcp-sidecar)
17. [🌐 Встраивание на сайт](#-встраивание-на-сайт)
18. [🚀 Быстрый запуск](#-быстрый-запуск)
19. [🐳 Запуск через Docker](#-запуск-через-docker)
20. [⚙️ Настройка](#️-настройка)
21. [🧰 Публичный API](#-публичный-api)
22. [📡 События](#-события)
23. [🧪 Тестирование](#-тестирование)
24. [🗂️ Документация по коду](#️-документация-по-коду)
25. [⚠️ Важные замечания](#️-важные-замечания)
26. [🛠️ Типовые изменения](#️-типовые-изменения)
27. [🐛 Диагностика ошибок](#-диагностика-ошибок)
28. [🤝 Развитие проекта](#-развитие-проекта)
29. [📄 Лицензия](#-лицензия)

---

## 📝 Описание

Graph Studio представляет собой **универсальный редактор и визуализатор графов связей**.

Граф состоит из двух базовых сущностей:

1. **Узел** — объект, понятие, группа, ядро или акцентный элемент.
2. **Связь** — отношение между двумя узлами.

Программа позволяет вручную создать структуру, настроить её внешний вид, увидеть результат в реальном времени, сохранить проект в JSON и встроить готовый граф на другой сайт.

Проект не привязан к магазину, товарам, рискам, положительным или отрицательным свойствам. Семантика графа полностью определяется пользователем. Один и тот же движок подходит для:

- структуры проекта;
- карты знаний;
- организационной схемы;
- зависимостей программных модулей;
- причинно-следственных связей;
- архитектуры информационной системы;
- учебной схемы;
- графа сущностей;
- карты заинтересованных сторон;
- визуализации процессов;
- связей между документами;
- любой другой сетевой структуры.

Главный принцип проекта:

> **Данные, визуализация, интерфейс, машинные команды и транспорт MCP разделены на независимые модули.**

Благодаря этому граф можно использовать целиком, частично или встроить в существующую систему без копирования административной панели.

---

## 🎯 Для чего нужна программа

Graph Studio решает несколько практических задач.

### 1. Ручное создание графа

Администратор добавляет узлы и связи через формы, меняет цвета, размеры, подписи, расположение и анимацию.

### 2. Визуальное исследование структуры

Пользователь вращает граф, масштабирует сцену, наводит курсор на узлы и получает подробную информацию.

### 3. Подготовка презентационного модуля

После настройки административный интерфейс можно скрыть и встроить только готовый граф.

### 4. Построение графа из данных ИИ

Модель формирует простой JSON формата `graph-studio/1`, после чего приложение проверяет данные и воспроизводит структуру.

### 5. Управление через атомарные команды

ИИ, скрипт или внешний интерфейс может выполнять понятные операции:

```text
add_node
add_connection
change_node
change_connection
set_layout
change_view
```

### 6. Управление через MCP

При необходимости локальный MCP-sidecar предоставляет те же команды как инструменты, ресурсы и prompts.

---

## ✨ Ключевые функции и возможности

### 🕸️ Редактор структуры

- добавление узлов;
- изменение узлов;
- удаление узлов вместе с принадлежащими им связями;
- добавление связей;
- изменение подписи, цвета, описания и толщины связи;
- удаление связи;
- запрет дублирования связи между одной парой узлов;
- импорт и экспорт JSON.

### 🎨 Настройка внешнего вида

- цвета всех типов узлов;
- индивидуальный цвет узла;
- индивидуальный размер узла;
- цвет и сила связи;
- подписи только ядер или всех узлов;
- легенда;
- фоновые точки;
- частицы глубины;
- информационные карточки.

### 🪐 Две раскладки

- **Планетарная паутина:** ветви расходятся от центрального ядра.
- **Желейные соты:** узлы размещаются в шестигранной сетке и реагируют на импульсы как упругая система.

### 🧲 Интерактивная физика

- инерция камеры;
- плавное вращение;
- плавное масштабирование;
- пружины между узлами;
- притяжение к исходным позициям;
- отталкивание;
- предотвращение наложений;
- передача импульса соседним узлам;
- перетаскивание узла;
- желейная реакция структуры.

### ❤️ Органическая пульсация

- двойной сердечный удар;
- волна от ядра по уровням графа;
- изменение размера и формы узлов;
- изменение ширины связи;
- каплевидный импульс по ветви;
- возможность полностью отключить свечение.

### 💨 Потоковые частицы

- движутся непосредственно по связи;
- имеют форму вытянутого штриха;
- имеют прозрачный хвост;
- не выглядят как дополнительные узлы;
- количество, скорость, длина и толщина настраиваются.

### 📐 Адаптивная толщина связи

Сильная связь может быть толстой в центре, но автоматически сужается около маленького узла. Поэтому визуальная сила отношения сохраняется, не перекрывая конечный элемент.

### 🔒 Режим администратора и просмотра

- администратор редактирует граф;
- пользователь видит только модуль;
- формы редактирования можно полностью скрыть;
- методы записи блокируются в viewer-режиме;
- вращение, масштабирование и карточки остаются доступными.

### 🤖 Машинно-понятный контракт

- строгий JSON-план;
- 13 атомарных команд;
- одинаковые имена команд в браузере, Web Component, `postMessage` и MCP;
- JSON Schema;
- предсказуемые ошибки;
- проверка уникальности ID и связей.

---

## 🧩 Состав проекта

| Компонент | Назначение |
|---|---|
| `index.html` | Полный административный интерфейс |
| `styles.css` | Оформление редактора и сцены |
| `editor.js` | Точка входа административного UI |
| `config.js` | Совместимый экспорт конфигурации |
| `3d-graph-engine.js` | Публичный facade движка |
| `graph-widget.js` | Публичный facade Web Component |
| `embed.html` | Минимальная iframe-страница |
| `src/engine/Graph3DEngine.js` | Главный движок сцены |
| `src/core/default-config.js` | Все значения по умолчанию |
| `src/core/graph-schema.js` | Проверка данных и структура графа |
| `src/layouts/layouts.js` | Планетарная и hex-раскладки |
| `src/math/projection.js` | Поворот и 3D-проекция |
| `src/animation/heart-pulse.js` | Математика сердечной волны |
| `src/render/organic-shapes.js` | Органические контуры узлов |
| `src/render/link-ribbon.js` | Сужающиеся связи и потоковые штрихи |
| `src/interaction/hit-test.js` | Поиск связи под курсором |
| `src/ui/app-controller.js` | Связь форм с движком |
| `src/ui/live-preview-controller.js` | Черновик, фиксация и отмена настроек |
| `src/ui/link-editor-controller.js` | Редактор связи при наведении |
| `src/ui/node-info-controller.js` | Информационная карточка узла |
| `src/ui/overlay-manager.js` | Разведение всплывающих окон |
| `src/ui/legend-controller.js` | Управление легендой |
| `src/model/ai-plan.js` | Формат `graph-studio/1` |
| `src/model/tool-registry.js` | Атомарные команды |
| `src/model/access-policy.js` | Ограничения viewer/admin |
| `src/embed/graph-widget.js` | Web Component |
| `src/embed/mount.js` | Упрощённое программное подключение |
| `model/*.json` | Машинные контракты и JSON Schema |
| `mcp/server.mjs` | Необязательный MCP-сервер |
| `mcp/project-store.mjs` | Безопасная запись проекта в JSON |
| `tests.js` | Браузерные DDT-тесты |
| `scripts/check-project.mjs` | Статическая проверка проекта |
| `scripts/runtime-engine-check.mjs` | Runtime-проверка движка без браузерного UI |
| `Dockerfile` | Контейнер Nginx |

---

## 🏗️ Архитектура

Проект построен слоями.

```text
┌──────────────────────────────────────────────────────────┐
│                         UI                               │
│ app-controller · node-info · link-editor · legend       │
└───────────────────────────┬──────────────────────────────┘
                            │ публичные методы и события
┌───────────────────────────▼──────────────────────────────┐
│                     Graph3DEngine                        │
│ состояние · камера · физика · рендер · интеракция        │
└──────────────┬────────────┬────────────┬─────────────────┘
               │            │            │
       ┌───────▼──────┐ ┌───▼──────┐ ┌──▼─────────────┐
       │ Layouts      │ │ Math     │ │ Render helpers │
       │ planetary    │ │ rotation │ │ ribbon/blob    │
       │ hex          │ │ project  │ │ flow           │
       └──────────────┘ └──────────┘ └────────────────┘
               │
┌──────────────▼───────────────────────────────────────────┐
│ Core: config · schema · utils · deterministic contracts │
└──────────────────────────────────────────────────────────┘

Отдельно:

model → AI JSON и атомарные команды
embed → Web Component и mount-функция
mcp   → необязательный транспорт к тому же словарю команд
```

### Правило зависимостей

Нижний слой не должен знать о верхнем.

Например:

- `projection.js` не ищет элементы HTML;
- `heart-pulse.js` не знает цвет узла;
- `Graph3DEngine.js` не строит формы;
- MCP-сервер не рисует Canvas;
- UI не рассчитывает физические силы самостоятельно.

Это позволяет тестировать математику отдельно и уменьшает вероятность того, что изменение кнопки разрушит раскладку. Человеческая цивилизация уже достаточно страдала от обработчиков `onclick`, содержащих половину приложения.

---

## 🧠 Модель данных

### Полный проект

```json
{
  "version": "8.0.0",
  "data": {
    "nodes": [],
    "links": []
  },
  "config": {}
}
```

### Узел

```json
{
  "id": "core",
  "name": "Главная тема",
  "type": "core",
  "description": "Подробное описание",
  "color": "#263846",
  "size": 25
}
```

| Поле | Тип | Обязательное | Назначение |
|---|---|---:|---|
| `id` | string | да | Уникальный стабильный идентификатор |
| `name` | string | рекомендуется | Отображаемое название |
| `type` | string | нет | `core`, `group`, `node`, `accent`, `default` |
| `description` | string | нет | Текст информационной карточки |
| `color` | string | нет | Индивидуальный цвет `#RRGGBB` |
| `size` | number | нет | Индивидуальный радиус узла |

### Связь

```json
{
  "source": "core",
  "target": "branch_1",
  "label": "содержит",
  "description": "Описание отношения",
  "color": "#8699a7",
  "width": 4
}
```

| Поле | Тип | Обязательное | Назначение |
|---|---|---:|---|
| `source` | string | да | ID первого узла |
| `target` | string | да | ID второго узла |
| `label` | string | нет | Краткая подпись отношения |
| `description` | string | нет | Подробное описание |
| `color` | string | нет | Индивидуальный цвет |
| `width` | number | нет | Центральная сила/толщина связи |

### Правило уникальности

Связи считаются неориентированными парами:

```text
A → B == B → A
```

Поэтому второй вариант создать нельзя.

Канонический ключ пары строится функцией:

```js
canonicalLinkKey('A', 'B'); // A::B
canonicalLinkKey('B', 'A'); // A::B
```

### Ограничения

- ID узла не может быть пустым;
- ID узла не может повторяться;
- связь должна указывать на существующие узлы;
- узел нельзя связать с самим собой;
- одна пара узлов может иметь только одну связь.

---

## 🕸️ Узлы и связи

### Типы узлов

| Тип | Смысл по умолчанию | Стандартный размер |
|---|---|---:|
| `core` | Центральное ядро | 25 |
| `group` | Крупная ветвь или раздел | 18 |
| `node` | Обычный элемент | 13 |
| `accent` | Акцентный элемент | 16 |
| `default` | Нейтральный резервный тип | 14 |

Тип влияет только на оформление и начальную роль в раскладке. Он не навязывает предметный смысл.

### Порядок создания

1. Создать центральный узел `core`.
2. Создать группы первого уровня.
3. Создать дочерние узлы.
4. Создать связи.
5. При необходимости добавить дополнительные поперечные отношения.

### Пример

```js
graph.run('add_node', {
  id: 'core',
  name: 'Информационная система',
  type: 'core'
});

graph.run('add_node', {
  id: 'security',
  name: 'Информационная безопасность',
  type: 'group'
});

graph.run('add_connection', {
  from: 'core',
  to: 'security',
  label: 'включает'
});
```

---

## 🪐 Раскладки

### Планетарная паутина

Значение:

```js
layout.type = 'planetary';
```

Принцип работы:

1. Ядро ставится в центр.
2. Алгоритм строит дерево от ядра.
3. Узлы первого уровня распределяются по окружности.
4. Дочерние узлы продолжают направление своей ветви.
5. Глубина добавляет небольшое смещение по оси Z.
6. После построения выполняется устранение столкновений.

Основные параметры:

```js
layout: {
  planetary: {
    ringGap: 190,
    branchSpread: 0.56,
    depth: 34
  }
}
```

- `ringGap` — расстояние между уровнями;
- `branchSpread` — угловой разброс дочерних ветвей;
- `depth` — смещение по глубине.

### Желейные соты

Значение:

```js
layout.type = 'hex';
```

Принцип работы:

1. Узлы сортируются по уровню и ID.
2. Генерируется аксиальная спираль шестигранной сетки.
3. Координаты `q/r` переводятся в экранные X/Y.
4. Чётные и нечётные кольца получают небольшую глубину Z.
5. Между соседними позициями создаются пружины.
6. Импульс одного узла распространяется по физической сети.

Основные параметры:

```js
layout: {
  hex: {
    gap: 112,
    depth: 14
  }
}
```

---

## 🧲 Физика и движение

Физика работает как комбинация сил.

### 1. Якорная сила

Возвращает узел к рассчитанной позицией раскладки.

```text
F_anchor = (target - position) × anchorStrength
```

### 2. Пружина связи

Удерживает связанные узлы на разумном расстоянии.

```text
F_spring = displacement × springStrength
```

### 3. Отталкивание

Не позволяет узлам слипаться.

```text
F_repulsion ∝ repulsionStrength / distance²
```

### 4. Демпфирование

Постепенно уменьшает скорость.

```text
velocity *= exp(-damping × dt)
```

### 5. Ограничение скорости

Защищает симуляцию от взрыва координат при большом импульсе.

### 6. Подшаги

Один визуальный кадр делится на несколько физических вычислений:

```js
physics.substeps = 3;
```

Это делает движение устойчивее при нестабильном FPS.

### Камера

Камера хранит:

- текущий угол X;
- текущий угол Y;
- целевой угол X;
- целевой угол Y;
- угловые скорости;
- текущий zoom;
- целевой zoom.

Из-за разделения текущих и целевых значений движение получается плавным.

---

## ❤️ Органическая пульсация

Сердечный цикл формируется функцией `heartBeat()`.

Он состоит из трёх гауссовых импульсов:

1. сильный первый удар;
2. более слабый второй удар;
3. мягкая фаза восстановления.

Упрощённо:

```text
beat = first + second × 0.62 + recovery × 0.12
```

### Передача по уровням

Для каждого узла определяется уровень от ядра.

```text
phase(node) = time × beatsPerSecond - level × branchDelay
```

Поэтому:

- ядро сокращается первым;
- узлы первого уровня реагируют позже;
- второй уровень получает ещё большую задержку;
- импульс визуально проходит по ветвям.

### Органическая форма

При стиле `organic` контур узла строится не идеальным кругом. Радиус каждой точки немного изменяется синусоидальными волнами.

Результат:

- узел «дышит»;
- форма мягко меняется;
- свечение не требуется;
- эффект воспринимается как физическое сокращение, а не мигающая лампочка.

### Настройка

```js
networkPulse: {
  enabled: true,
  style: 'organic',
  bpm: 62,
  branchDelay: 0.115,
  travelWindow: 0.48,
  nodeAmplitude: 0.15,
  nodeDeformation: 0.06,
  linkWidthBoost: 1.35,
  glowEnabled: false,
  markerSize: 3.8
}
```

---

## 💨 Поток по ветвям

Постоянный поток отделён от сердечного импульса.

Сердечный импульс показывает периодическую волну, а `links.flow` создаёт непрерывное движение.

```js
links: {
  flow: {
    enabled: true,
    count: 4,
    speed: 0.28,
    size: 2.2,
    opacity: 0.92,
    trailLength: 0.085,
    shape: 'streak'
  }
}
```

Каждая частица:

1. получает собственное смещение по фазе;
2. движется от начала связи к концу;
3. проходит easing-функцию;
4. рисуется короткой линией с градиентным хвостом;
5. не имеет круглой головы, если `headDot: false`.

---

## 📐 Сужающиеся связи

Обычная Canvas-линия имеет одинаковую толщину везде. В Graph Studio связь строится как замкнутая лента.

### Геометрия

Для двух узлов вычисляются:

- единичный вектор вдоль связи;
- перпендикулярный вектор;
- начало после отступа от первого узла;
- конец до границы второго узла;
- центральная точка;
- ширина в начале;
- ширина в середине;
- ширина в конце.

### Ограничение конца

```text
endpointWidth = min(middleWidth, nodeRadius × endpointRatio)
```

То есть связь может быть сильной в центре, но становится тоньше у маленького узла.

### Настройка

```js
links: {
  taper: {
    enabled: true,
    endpointRatio: 0.34,
    minEndpointWidth: 0.55,
    insetRatio: 0.82
  }
}
```

---

## 🖥️ Интерфейс

Административная панель разделена на вкладки.

### Структура

- создание узла;
- создание связи;
- списки элементов;
- импорт;
- экспорт;
- очистка;
- демонстрационный пример.

### Движение

- выбор раскладки;
- расстояния;
- упругость;
- затухание;
- инерция;
- скорость вращения;
- углы камеры.

### Оформление

- цвета;
- размеры;
- подписи;
- пульсация;
- поток;
- фон;
- частицы;
- информационная карточка.

### Легенда

- включение;
- заголовок;
- позиция;
- добавление пунктов;
- удаление пунктов;
- форма маркера.

### ИИ / JSON

- JSON-план;
- атомарные команды;
- текущая конфигурация;
- встроенная консоль.

### Справка

Краткое объяснение режимов и управления.

---

## 👁️ Режим просмотра

Graph Studio является модулем, поэтому публичный просмотр рекомендуется встраивать как Web Component или iframe.

В режиме `viewer`:

- административная панель отсутствует;
- узлы нельзя изменять;
- связи нельзя изменять;
- команда записи блокируется;
- редактор связи не открывается;
- камера и информационные карточки работают.

Пример:

```html
<graph-studio-widget
  src="/data/project.json"
  mode="viewer"
  height="620px"
  controls>
</graph-studio-widget>
```

### Важное ограничение

Viewer-режим защищает обычный интерфейс, но не заменяет серверную авторизацию. Если данные записываются через backend, права доступа обязан проверять сервер.

---

## 🤖 Работа с ИИ без MCP

Рекомендуемый путь — один декларативный JSON.

```json
{
  "format": "graph-studio/1",
  "title": "Архитектура системы",
  "nodes": [
    {
      "id": "core",
      "name": "Система",
      "type": "core"
    },
    {
      "id": "backend",
      "name": "Backend",
      "type": "group"
    },
    {
      "id": "database",
      "name": "База данных",
      "type": "node"
    }
  ],
  "connections": [
    {
      "from": "core",
      "to": "backend",
      "label": "включает"
    },
    {
      "from": "backend",
      "to": "database",
      "label": "использует"
    }
  ],
  "view": {
    "layout": "planetary",
    "mode": "admin"
  }
}
```

### Правила для модели

1. Использовать один основной `core`.
2. Использовать короткие ID без пробелов.
3. Сначала создать все узлы.
4. Затем создать связи.
5. Не ссылаться на отсутствующие ID.
6. Не дублировать связь в обратном направлении.
7. Короткое название помещать в `name`.
8. Подробности помещать в `description`.
9. Не придумывать оценочные категории без указания пользователя.
10. Для полной структуры использовать JSON-план, а не десятки команд.

### Атомарные команды

| Команда | Что делает |
|---|---|
| `get_project` | Получает весь проект |
| `replace_graph` | Полностью заменяет узлы и связи |
| `add_node` | Добавляет узел |
| `change_node` | Изменяет узел |
| `delete_node` | Удаляет узел |
| `add_connection` | Добавляет связь |
| `change_connection` | Изменяет связь |
| `delete_connection` | Удаляет связь |
| `set_layout` | Выбирает раскладку |
| `change_view` | Меняет настройки отображения |
| `set_read_only` | Включает блокировку |
| `add_legend_item` | Добавляет пункт легенды |
| `delete_legend_item` | Удаляет пункт легенды |

---

## 🔌 MCP-sidecar

MCP в проекте является необязательным транспортным слоем.

Основное приложение работает без него.

### Зачем нужен sidecar

- дать модели структурированные инструменты;
- предоставить текущий проект как resource;
- предоставить схемы команд;
- хранить изменения в JSON-файле;
- выполнять атомарную запись.

### Запуск

```bash
cd mcp
npm install
npm start
```

### Пользовательский файл проекта

PowerShell:

```powershell
$env:GRAPH_PROJECT_FILE = "D:\graphs\project.json"
npm start
```

Linux/macOS:

```bash
GRAPH_PROJECT_FILE=/home/user/graphs/project.json npm start
```

### Безопасная запись

`ProjectStore` пишет проект во временный файл, а затем выполняет `rename()`.

Это уменьшает риск получить наполовину записанный JSON при прерывании процесса.

---

## 🌐 Встраивание на сайт

### Web Component

```html
<script type="module" src="/graph-studio/graph-widget.js"></script>

<graph-studio-widget
  id="knowledge-map"
  src="/data/graph-project.json"
  height="620px"
  layout="planetary"
  mode="viewer"
  controls>
</graph-studio-widget>
```

### Программное подключение

```html
<div id="graph-host"></div>

<script type="module">
  import { mountGraphStudio } from '/graph-studio/graph-widget.js';

  const widget = mountGraphStudio('#graph-host', {
    src: '/data/graph-project.json',
    height: '620px',
    mode: 'viewer',
    layout: 'hex',
    controls: true
  });
</script>
```

### iframe

```html
<iframe
  src="/graph-studio/embed.html?src=/data/graph-project.json"
  title="Граф связей"
  style="width:100%;height:620px;border:0">
</iframe>
```

### Изменение данных владельцем страницы

```js
const widget = document.querySelector('graph-studio-widget');

widget.setData({ nodes, links });
widget.updateConfig({
  legend: { enabled: false }
});
```

---

## 🚀 Быстрый запуск

### Вариант 1. Python

```powershell
cd "D:\путь\к\graph-mini-project-v8"
py -m http.server 8080
```

Открыть:

```text
http://localhost:8080
```

### Вариант 2. Node.js

Можно использовать любой статический сервер, например:

```bash
npx serve .
```

### Почему нельзя всегда открывать `index.html` двойным кликом

Проект использует ES Modules. При протоколе `file://` браузер может заблокировать импорты или загрузку JSON. Нужен обычный HTTP-сервер.

---

## 🐳 Запуск через Docker

### Сборка

```powershell
docker build --no-cache -t graph-manual:v8 .
```

### Запуск

```powershell
docker run --rm --name graph-manual-v8 -p 8080:80 graph-manual:v8
```

### Открыть

```text
http://localhost:8080
```

### Проверить контейнер

```powershell
docker ps
```

### Остановить

```powershell
docker stop graph-manual-v8
```

### Что делает Dockerfile

1. Берёт `nginx:1.27-alpine`.
2. Копирует проект в `/usr/share/nginx/html`.
3. Создаёт минимальную конфигурацию Nginx.
4. Запрещает устаревший кеш HTML, JS, CSS и JSON.
5. Открывает порт 80.
6. Добавляет healthcheck.

---

## ⚙️ Настройка

Главный источник настроек:

```text
src/core/default-config.js
```

Совместимый корневой файл:

```text
config.js
```

### Пример частичного изменения

```js
engine.updateConfig({
  camera: {
    autoRotate: true,
    autoRotateSpeed: 0.02
  },
  particles: {
    enabled: false
  }
});
```

`deepMerge()` изменит только указанные поля, сохранив остальные значения.

### Live-preview

Поля движения и оформления применяются сразу как черновик.

```text
ползунок → transient preview → Canvas
```

После нажатия **«Применить»** изменения фиксируются.

После нажатия **«Отменить»** возвращается последняя сохранённая конфигурация.

Полная таблица параметров находится в:

```text
docs/CONFIG_REFERENCE.md
```

---

## 🧰 Публичный API

### Создание движка

```js
import {
  Graph3DEngine,
  DEFAULT_GRAPH_CONFIG
} from './3d-graph-engine.js';

const engine = new Graph3DEngine({
  canvas: document.querySelector('#graph-canvas'),
  config: DEFAULT_GRAPH_CONFIG,
  data: {
    nodes: [],
    links: []
  }
});

engine.start();
```

### Основные методы

```js
engine.setData(data);
engine.addNode(node);
engine.updateNode(id, changes);
engine.removeNode(id);

engine.addLink(link);
engine.updateLink(source, target, changes);
engine.removeLink(source, target);

engine.setLayout('planetary');
engine.updateConfig(patch);
engine.setCameraAngles(x, y);

engine.pause();
engine.resume();
engine.resetCamera();
engine.destroy();
```

### Экспорт

```js
const project = engine.exportData();
```

Подробное описание методов находится в:

```text
docs/API_REFERENCE.md
```

---

## 📡 События

Движок отправляет события через Canvas.

| Событие | Когда возникает |
|---|---|
| `graph:datachange` | Изменены узлы или связи |
| `graph:configchange` | Изменена конфигурация |
| `graph:hoverchange` | Изменился объект под курсором |
| `graph:linkactivate` | Активирована связь |
| `graph:lockchange` | Изменена блокировка |
| `graph:statechange` | Изменилось состояние анимации/камеры |

Пример:

```js
canvas.addEventListener('graph:datachange', (event) => {
  console.log(event.detail);
});
```

Временные preview-события имеют признак:

```js
event.detail.transient === true
```

---

## 🧪 Тестирование

### Статическая проверка

```bash
npm run check
```

Проверяются:

- синтаксис JavaScript;
- относительные импорты;
- JSON-файлы;
- структура проекта;
- наличие ключевых файлов.

### Браузерные тесты

Открыть:

```text
http://localhost:8080/tests.html
```

### Runtime-проверка движка

```bash
node scripts/runtime-engine-check.mjs
```

### Что тестируется

- валидация узлов;
- запрет повторных ID;
- запрет петель;
- запрет дублирующих связей;
- обратный дубль `B → A`;
- планетарная раскладка;
- hex-раскладка;
- проекция;
- столкновения;
- сердечная волна;
- сужение связи;
- блокировка редактирования;
- жизненный цикл движка;
- отсутствие `NaN` после физической симуляции.

---

## 🗂️ Документация по коду

В документированной сборке добавлены следующие файлы.

| Файл | Содержание |
|---|---|
| `README.md` | Основное описание проекта |
| `docs/FULL_PROGRAM_DESCRIPTION.md` | Расширенная техническая документация |
| `docs/MODULE_REFERENCE.md` | Каждый модуль, его зависимости и функции |
| `docs/API_REFERENCE.md` | Публичные методы и события |
| `docs/CONFIG_REFERENCE.md` | Все параметры конфигурации |
| `docs/DATA_FORMAT_REFERENCE.md` | Форматы данных и JSON-контракты |
| `docs/LINE_BY_LINE_REFERENCE.md` | Построчный разбор значимых исходников |
| `AI_GUIDE.md` | Инструкция для ИИ |
| `MCP_ARCHITECTURE.md` | MCP-sidecar |
| `EMBEDDING.md` | Встраивание |
| `CODE_GUIDE.md` | Краткое руководство разработчика |

---

## ⚠️ Важные замечания

### 1. Не редактируйте `package-lock.json` вручную

Он создаётся npm автоматически.

### 2. Не дублируйте `config.js` и `default-config.js`

Канонический файл находится в `src/core/default-config.js`. Корневой `config.js` оставлен для совместимости.

### 3. Не обращайтесь к внутренним массивам движка

Используйте:

```js
engine.addNode();
engine.updateNode();
engine.addLink();
engine.exportData();
```

### 4. Не создавайте обработчики DOM внутри математических модулей

`projection.js`, `heart-pulse.js`, `layouts.js` и `link-ribbon.js` должны оставаться чистыми.

### 5. Не используйте viewer как серверную защиту

Viewer скрывает редактирование на клиенте, но backend обязан самостоятельно проверять права.

### 6. Не разрешайте ИИ генерировать произвольный JavaScript

Предпочтительно использовать JSON-план или атомарные команды.

### 7. После обновления версии очистите кеш

```text
Ctrl + Shift + R
```

---

## 🛠️ Типовые изменения

### Сделать вращение медленнее

```js
camera: {
  autoRotateSpeed: 0.01
}
```

### Отключить автоматическое вращение

```js
camera: {
  autoRotate: false
}
```

### Отключить фоновые частицы

```js
particles: {
  enabled: false
}
```

### Увеличить ядро

```js
node: {
  sizes: {
    core: 34
  }
}
```

### Показывать названия всех узлов

```js
node: {
  labels: {
    mode: 'all'
  }
}
```

### Отключить сердечную волну

```js
networkPulse: {
  enabled: false
}
```

### Оставить органическую пульсацию только ядра

```js
networkPulse: {
  enabled: false
},
node: {
  corePulse: {
    enabled: true,
    style: 'organic'
  }
}
```

### Усилить сужение связи

```js
links: {
  taper: {
    endpointRatio: 0.22
  }
}
```

### Сделать поток быстрее

```js
links: {
  flow: {
    speed: 0.5
  }
}
```

---

## 🐛 Диагностика ошибок

### `Unexpected end of input`

JavaScript-файл обрезан или не полностью скопирован.

Проверить:

```powershell
node --check src\engine\Graph3DEngine.js
```

### `404 Not Found`

Проверить прямые адреса:

```text
http://localhost:8080/editor.js
http://localhost:8080/3d-graph-engine.js
http://localhost:8080/src/engine/Graph3DEngine.js
```

### `Unable to find image ... locally`

Имя запускаемого Docker-образа отличается от имени при сборке.

```powershell
docker image ls
```

### `TLS handshake timeout`

Docker не смог скачать базовый образ. Повторить:

```powershell
docker pull nginx:1.27-alpine
```

### Изменения не видны

1. Нажать **«Применить»**.
2. Выполнить `Ctrl + Shift + R`.
3. Убедиться, что используется версия v8.
4. Пересобрать Docker без кеша.

### Связь не добавляется

Скорее всего эта пара узлов уже соединена. `A → B` и `B → A` считаются одной связью.

---

## 🤝 Развитие проекта

При добавлении новой функции рекомендуется следующий порядок:

1. Создать чистый модуль или публичный метод.
2. Добавить проверку входных данных.
3. Добавить unit/DDT-тест.
4. Подключить функцию к движку.
5. Добавить UI.
6. Добавить машинную команду, если действие должно быть доступно ИИ.
7. Обновить JSON Schema.
8. Обновить документацию.
9. Проверить viewer-режим.
10. Проверить embed-компонент.

### Хороший модуль

- выполняет одну задачу;
- имеет понятное имя;
- не обращается к лишним слоям;
- получает зависимости через параметры или import;
- возвращает предсказуемый результат;
- выдаёт понятную ошибку;
- тестируется отдельно.

---

## 📄 Лицензия

Проект распространяется по свободной лицензии **MIT**.

Вы можете свободно:

- использовать программу;
- изменять исходный код;
- распространять копии;
- применять проект в коммерческих и некоммерческих целях;
- встраивать Graph Studio в другие сайты и приложения.

При распространении необходимо сохранить текст лицензии и уведомление об авторских правах.

Полный текст лицензии находится в файле [`LICENSE`](LICENSE).

---

**Автор:** Oganes Davtian  
**Лицензия:** MIT  
**Статус:** Свободное программное обеспечение



---

## 📌 Сведения о проекте

**Название:** Graph Studio  
**Версия:** v8.0.0  
**Тип:** автономный веб-модуль и редактор графов связей  
**Рендеринг:** Canvas 2D с перспективной 3D-проекцией  
**Язык:** JavaScript ES Modules  
**Развёртывание:** статический HTTP, Docker/Nginx, Web Component, iframe  
**ИИ-интеграция:** JSON-план, атомарные команды, необязательный MCP-sidecar  
**Статус:** активная версия, готовая к дальнейшей проверке и развитию
**Автор:** Oganes Pak

