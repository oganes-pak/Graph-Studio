/**
 * Совместимая публичная точка импорта конфигурации.
 * Канонический объект находится только в src/core/default-config.js,
 * чтобы корневой файл и движок никогда не расходились по значениям.
 */
export { DEFAULT_GRAPH_CONFIG } from './src/core/default-config.js';
